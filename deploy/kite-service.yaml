apiVersion: v1
kind: Service
metadata:
  name: kite-service
  namespace: kite-system
spec:
  type: ClusterIP
  selector:
    app: kite
  ports:
  - name: http
    port: 8080
    targetPort: 8081  # Updated to match the new Kite port
    protocol: TCP
